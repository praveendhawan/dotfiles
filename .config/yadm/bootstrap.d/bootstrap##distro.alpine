#!/bin/bash

# Install Testing repo packages
apk add oh-my-zsh tealdeer --repository=http://dl-cdn.alpinelinux.org/alpine/edge/testing/

# Install nerd fonts
apk add font-fira-mono-nerd

# basic utilities
apk add util-linux pciutils hwdata-pci usbutils hwdata-usb coreutils binutils findutils grep iproute2

# Install brew dependencies
apk add bat eza lnav jq procs fd fzf tree-sitter neovim ripgrep tree wget yarn sd starship aws-cli shared-mime-info libpq

# Install asdf
git clone https://github.com/asdf-vm/asdf.git ~/.asdf --branch v0.14.0
bash "$HOME/.asdf/asdf.sh"

sudo chsh -s /bin/bash root
sudo chsh -s /bin/zsh praveen

if [ ! -L /usr/local/bin/fd ]; then
  ln -s "$(which fdfind)" /usr/local/bin/fd
fi

nvim

apk add oh-my-zsh --repository=http://dl-cdn.alpinelinux.org/alpine/edge/testing/
